import 'package:flutter/material.dart';

class Auto_parts extends StatelessWidget {
@override
Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(
      title: Text("Auto-parts"),
    ),
    drawer: Drawer(),
    body: Center(
      child: Text("hethi les auto parts") ,
    ),

  );
}
}
